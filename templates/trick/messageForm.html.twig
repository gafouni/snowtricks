{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.content, { 'attr': {'placeholder': 'Entrez votre commentaire...'}}) }}

    
    {# {{ form_widget(form) }} #}
    <button class="btn btn-lg btn-secondary" style="margin:2rem;">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
