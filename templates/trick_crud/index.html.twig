{# {% extends 'base.html.twig' %}

{% block title %}Trick index{% endblock %}

{% block body %}
    <h1>Trick index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TrickName</th>
                <th>Description</th>
                <th>ImgFile</th>
                <th>VideoFile</th>
                <th>Slug</th>
                <th>CreatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for trick in tricks %}
            <tr>
                <td>{{ trick.id }}</td>
                <td>{{ trick.trickName }}</td>
                <td>{{ trick.description }}</td>
                <td>{{ trick.imgFile }}</td>
                <td>{{ trick.videoFile }}</td>
                <td>{{ trick.slug }}</td>
                <td>{{ trick.createdAt ? trick.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_trick_crud_show', {'id': trick.id}) }}">show</a>
                    <a href="{{ path('app_trick_crud_edit', {'id': trick.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_trick_crud_new') }}">Create new</a>
{% endblock %} #}
