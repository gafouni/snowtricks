{% extends 'base.html.twig' %}

{% block title %}Password Reset Email Sent{% endblock %}

{% block body %}

<!-- Contact-->
    <section class="page-section" id="contact">
        <div class="container">
            <div class="text-center" style="margin:3rem;">
 
                <p>
                    S'il existe un compte correspondant a votre email, alors vous venez de recevoir
                    un lien vous permettant de reinitialiser votre mot de passe.
                    {# If an account matching your email exists, then an email was just sent 
                    that contains a link that you can use to reset your password. #}
                    Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p>
                    Si vous n'avez rien recu, regardez dans le dossier spam ou
                    {# If you don't receive an email please check your spam folder or #}
                    <a href="{{ path('forgot_password_request') }}" style="color:blue;">Reessayer</a>.
                </p>
            </div>
        </div>
    </section>

{% endblock %}

